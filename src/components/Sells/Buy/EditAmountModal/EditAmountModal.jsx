import Modal from '../../../../UI/Modal'

const EditAmountModal = ({ close }) => {
  return <Modal onClose={close}>This is the modal to edit the amount of the product</Modal>
}

export default EditAmountModal
